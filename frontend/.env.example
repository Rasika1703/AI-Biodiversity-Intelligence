# Leave empty to use the dev proxy / same-origin deployment.
# Set to your deployed backend when the frontend is hosted separately, e.g.
# VITE_API_URL=https://darukaa-api.onrender.com/api
VITE_API_URL=
